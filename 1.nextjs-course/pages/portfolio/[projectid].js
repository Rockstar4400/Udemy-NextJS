import { useRouter } from 'next/router';

function PortfolioProjectPage () {
    const router = useRouter();

    router.

    return(
        <div>Dinamic Portafolio</div>
    );
}

export default PortfolioProjectPage;